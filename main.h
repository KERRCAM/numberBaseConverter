#include <stdio.h>
#include <string.h>
#include <math.h>
#include <ctype.h>

int intInput(char prompt[]);

const char* stringInput(char prompt[]);

int toBase10(char initialNumber[], int base);

int main();